import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  carousel: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  textWrapper: {
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    fontSize: 18,
    color: '#ffffff',
    textAlign: 'center',
    overflow: 'hidden',
  },
});
